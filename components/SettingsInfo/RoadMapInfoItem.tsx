import React from "react";

type Props = {
  color: string;
  text: string;
  amount: number;
};

const RoadMapInfoItem = ({ color, text, amount }: Props) => {
  return <div>RoadMapInfoItem</div>;
};

export default RoadMapInfoItem;
